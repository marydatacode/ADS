// Tipos de Dados:
    // Boolean
    // String
    // Number

var nome = 23;
console.log(typeof(nome));// falar o tipo de dado

if(typeof(nome) == "string") {
    console.log(nome.toLocaleUpperCase()); //colocoar em capslock
} else {
    console.log('vocee precisa usar uma string')
}
