// ARRAY
    // é uma lista de objetos
    // objetos que contemmultiplos valores armazenados em uma lista
    // um objeto array pode ser armazenado em variaveis e ser tratados de forma muito similiar a qualquer outro tipo de valor
    // nao possui um tipo de dados array especifco
        var = [item1, item2, item3,...]
    // monta uma lista de mercado por ex em uma array
         var sequencia = [1,2,3];
         var aleatorio = ['arvore',321, [1,34]];
         var arr = new Array(elmen1, elemn2);
         var art = Array(elem4, elem5);
         array [0] vai ser elem4

        var are = [42] // array com apenas um elemento e ele é 42
        var are = Array(42) // Cria um array sem elementos e are.length é definido como 42
        