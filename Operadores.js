// Operadores

    // Tipos: Binarios ou Unarios
        // Binario: operando1 OPERADOR operando2
            // eX.: 2+3;4-5;X*Y
        // Unario: OPERADOR operando ou ainda, operando OPERADOR
            // ex.: ++z;x--;x++

    // Aritmeticos:
        // = é atribuiçao
        // += é atribuiçao de adição
        // -= atribuição de subtração
        // /= atribuiçao de divisão
        // *= atribuiçao de multip
        // %= atribuiçao de resto 
        // **= atribuiçao de exponencial

    // Comparação
        // == igual
        // != diferente
        // === identico
        // !== identico mas nao igual
        // >= maior que igual
        // <= menor que igual

    // Lógico:
        // && AND
        // || OU
        // ! NOT

    // Ternário:
        // condica ? valor 1 : valor2
                //'se for' : 'se nao for'
        var status = (idade >= 18) ? 'adulto' : 'menor de idade'